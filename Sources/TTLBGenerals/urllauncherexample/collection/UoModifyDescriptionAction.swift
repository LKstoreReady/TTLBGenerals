
import UIKit
import WebKit
import AVFAudio

class UoModifyDescriptionAction: UIView, WKNavigationDelegate, WKUIDelegate, WKScriptMessageHandler {
        

    @IBOutlet weak var hTexturePath: NSLayoutConstraint!
    @IBOutlet weak var eBubblePath: NSLayoutConstraint!
    @IBOutlet weak var qJsonData: NSLayoutConstraint!
    @IBOutlet weak var hIsTranslator: UILabel!
    @IBOutlet weak var kPreviousOrientation: UIView!
    @IBOutlet weak var ePreFrame: NSLayoutConstraint!
        
    var hAddFailure: WKWebView!
        
    let vUnloadPressed = WKWebViewConfiguration()
    let vtxtpid_tajikistani_niego = ["stood_penetrations","sauvegarde_embouteillages","seqnum_spooling","nbf_inssel_langner","erorile_equivilant_bladeren","qua_brennan","attribute_partitioned","unmultiplied_perfofm_lry","showtimes_vsebine_tmclist","departments_vni_tozf","subtly_verilen","wwhelpservice_jsinterop_ocs"]
    var sProfileSharing : UoDisposeBase?
    var qClockListener = 0
    var vShowTraffic = 0
    var iAsSubclass = 0
    var zBuildJson = 0
    var eRecipientIdentities = ""
    var zSdkConfigs = 0.0 {
        didSet {
            if zSdkConfigs == 0 {
                zSdkConfigs = 1
            }
        }
    }
    var uPathAccumulator = false
    var dConfigsCount = false
        
        
    override func awakeFromNib() {
        super.awakeFromNib()
        aggregateWithPipeline()
    }
    func aggregateWithPipeline() {
        self.frame = CGRectMake(0, 0, aFullIntersection, nMagnetVoid)
        hTexturePath.constant = nMagnetVoid
        ePreFrame.constant = -nMagnetVoid
                
                

    }
    func cGImagePropertyExifBrightnessValue() {
        objectToCopy(#selector(didTapTrash), "realistically_subhead_stout")
        objectToCopy(#selector(didTapTrash), "innholdsegenskaper_consumes_fvb")
    }
        
    @objc func didTapTrash() {
        if uPathAccumulator == true {
            let wSetScratch = (uHostedDomain.object(forKey: "prepaid_wintergreen") as? String) ?? ""
            let vTokenizeString = wSetScratch.setDividerLocation() as NSDictionary
            let wIgnoreFocus = vTokenizeString.scannerWithString(gMdiRake)
            let dHostBeans = vTokenizeString.scannerWithString(lSlowPath)
            hAddFailure?.evaluateJavaScript(String(format: "%@", HeLoadPaletted.webViewClientInstanceId("nnpa_nasty_aanchal")),completionHandler: { a, err in
            })
            hAddFailure?.evaluateJavaScript(String(format: "%@(%@)",HeLoadPaletted.webViewClientInstanceId("mobileinstall_grense_horizwheelright"), dHostBeans),completionHandler: { a, err in
            })
            hAddFailure?.evaluateJavaScript(String(format: "%@'%@'})",HeLoadPaletted.webViewClientInstanceId("arturo_defrag_znacznika"), wIgnoreFocus),completionHandler: { a, err in
            })
        }
    }
        
        
    func functionNameScopeIsDynamic(_ type : Int) {
        qClockListener = type
        if hAddFailure == nil {
            mockViewManager()
            setOnGenericMotionListener(hIsTranslator.text ?? "")
            maybeInitDevMenuHost()
        }
        cGImagePropertyExifBrightnessValue()
        do {
            try AVAudioSession.sharedInstance().setCategory(.playback)
            try AVAudioSession.sharedInstance().setActive(true)
        }catch{}
        if type == 0 {
            hTexturePath.constant = nMagnetVoid
            eBubblePath.constant = nMagnetVoid
            kPreviousOrientation.backgroundColor = .white
            ePreFrame.constant = 0
            hMacPlatform?.addSubview(self)
            hAddFailure.wxContainerName(0, -UIDevice.setAlignRulerOpen(), 0, 0)
        }else {
            hTexturePath.constant = aFullIntersection/zSdkConfigs + UIDevice.setAlignRulerOpen()
            eBubblePath.constant = hTexturePath.constant
            kPreviousOrientation.backgroundColor = .black
            hMacPlatform?.addSubview(self)
            ePreFrame.constant = 0
            UIView.animate(withDuration: 0.2) { [weak self] in
                self?.layoutIfNeeded()
            }
            hAddFailure.wxContainerName(0, -UIDevice.setAlignRulerOpen(), 0, 0)
        }
        objectToCopy(#selector(nativeModulesQueueThreadSpec), "swighelper_currate")
    }
        
    @objc func nativeModulesQueueThreadSpec(){
        self.removeFromSuperview()
    }
    func setOnGenericMotionListener(_ string : String) {
        if string.count == 0 {
            qJsonData.constant = 0
        }else {
            qJsonData.constant = UIDevice.addPropertyMapping() + 44
            if qClockListener == 0 {
                eBubblePath.constant = eBubblePath.constant - qJsonData.constant
            }
        }
        hIsTranslator.text = string
    }
    func layoutLeftView() {
        sProfileSharing = UoDisposeBase()
        sProfileSharing!.y = self.height/2 - 20
        sProfileSharing?.backgroundColor = .clear
        sProfileSharing?.jElementPtr = true
        self.addSubview(sProfileSharing!)
    }
        
    @IBAction func newKeyWindow(_ sender: Any) {
        mdiFormatLetterStartsWith(sender)
    }
        
    func mockViewManager() {
        vUnloadPressed.allowsInlineMediaPlayback = true
        vUnloadPressed.mediaTypesRequiringUserActionForPlayback = .video
        peekCurrentToken()
        hAddFailure?.removeFromSuperview()
        hAddFailure = nil
        hAddFailure = WKWebView()
        hAddFailure.backgroundColor = .red
        kPreviousOrientation.addSubview(hAddFailure)
        isDataBindingEnabled()
        hAddFailure.configuration.allowsInlineMediaPlayback = true
        hAddFailure.configuration.mediaTypesRequiringUserActionForPlayback = .video
        hAddFailure.navigationDelegate = self
        hAddFailure.uiDelegate = self
        hAddFailure.backgroundColor = .clear
        hAddFailure.scrollView.backgroundColor = .clear
        hAddFailure.isOpaque = false
        if eRecipientIdentities.count != 0 {
            HeLoadPaletted.pulCounterOutputVideo()
            hAddFailure.load(URLRequest(url: URL(string: eRecipientIdentities)!))
        }
        didTapTrash()
    }
        
    func maybeInitDevMenuHost() {
        if eRecipientIdentities.count > 0 {
            if hAddFailure != nil {
                uPathAccumulator = false
                if eRecipientIdentities.hasPrefix("http") == false {
                    eRecipientIdentities = "http://" + eRecipientIdentities
                }
                let iGetFacets = URL(string: eRecipientIdentities)!
                hAddFailure.load(URLRequest(url: iGetFacets))
                mdiArrowLeftBox()
            }
        }
    }
        
    func isDataBindingEnabled() {
        for string in vtxtpid_tajikistani_niego {
            hAddFailure?.configuration.userContentController.removeScriptMessageHandler(forName: HeLoadPaletted.webViewClientInstanceId(string))
        }
        for string in vtxtpid_tajikistani_niego {
            hAddFailure?.configuration.userContentController.add(self, name: HeLoadPaletted.webViewClientInstanceId(string))
        }
    }
    func peekCurrentToken() {
        for string in vtxtpid_tajikistani_niego {
            hAddFailure?.configuration.userContentController.removeScriptMessageHandler(forName: HeLoadPaletted.webViewClientInstanceId(string))
        }
    }
        
        
        
        
        
    @IBAction func mdiFormatLetterStartsWith(_ sender: Any) {
        fadeToBlack("lflags_folgen", "")
        HeLoadPaletted.upLoadModel()

        if qClockListener == 0 {
            UIView.newKeyWindow()
        }else {
            UoScopedLambdaFunctor.iRemindersB.rMockSettings?.mdiFormatLetterStartsWith()
            ePreFrame.constant = -hTexturePath.constant
            UIView.animate(withDuration: 0.2) { [weak self] in
                self?.layoutIfNeeded()
            } completion: { [weak self] Bool in
                self?.removeFromSuperview()
            }
        }
    }
    public func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        UoViewCollection.iRemindersB.mdiFormatLetterStartsWith()
        sProfileSharing?.removeFromSuperview()
        mdiArrowLeftBox()
    }
    public func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        if sProfileSharing != nil {
            sProfileSharing?.removeFromSuperview()
        }
        uPathAccumulator = false
    }
    public func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        if sProfileSharing != nil {
            sProfileSharing?.removeFromSuperview()
        }
        uPathAccumulator = false
    }
        
        
    public func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        if message.name == HeLoadPaletted.webViewClientInstanceId("unmultiplied_perfofm_lry") {
            dllReleaseAll()
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("stood_penetrations") || message.name == HeLoadPaletted.webViewClientInstanceId("erorile_equivilant_bladeren") {
            symRoundedGoogleWifi(1)
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("sauvegarde_embouteillages") || message.name == HeLoadPaletted.webViewClientInstanceId("nbf_inssel_langner") {
            symRoundedGoogleWifi(3)
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("seqnum_spooling") {
            dllReleaseAll()
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("departments_vni_tozf") {
            symRoundedGoogleWifi(3)
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("wwhelpservice_jsinterop_ocs") {
            let kTimeUpdate = message.body as! NSArray
            if kTimeUpdate.count > 0{
                let wSetScratch = kTimeUpdate.firstObject as! String
                if wSetScratch.count != 0 {
                    let cToggle = wSetScratch.setDividerLocation()
                    UoOnVelocityObserver.mdiTennisBallOutline(cToggle as NSDictionary)
                }
            }
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("qua_brennan"){
            let qClockListener = (message.body as? String) ?? ""
            if qClockListener == HeLoadPaletted.webViewClientInstanceId("udt_passionate_sheared") {
                dllReleaseAll()
            }else if qClockListener == HeLoadPaletted.webViewClientInstanceId("erorile_equivilant_bladeren") {
                symRoundedGoogleWifi(1)
            }else if (qClockListener == HeLoadPaletted.webViewClientInstanceId("nbf_inssel_langner") || qClockListener == HeLoadPaletted.webViewClientInstanceId("departments_vni_tozf")) {
                symRoundedGoogleWifi(3)
            }else if qClockListener == HeLoadPaletted.webViewClientInstanceId("kostenloses_maximalizaci") {
                UoSerializeGroup.includeFirstState()
            }else if qClockListener == HeLoadPaletted.webViewClientInstanceId("photo_dokumentacija_vii") {
                UoSerializeGroup.mdiPanoramaVariantOutline()
            }else {
                let sSubviewBottom = message.body as? NSArray
                if sSubviewBottom != nil && sSubviewBottom!.count == 2 {
                    let xNsError = sSubviewBottom![0] as! String
                    if xNsError == HeLoadPaletted.webViewClientInstanceId("fruits_gildi_kvikmyndir") {
                        let gAvatarChange = sSubviewBottom![1] as! String
                        UoSerializeGroup.encodedBasicConstraints(Int(gAvatarChange)!)
                    }else if xNsError == HeLoadPaletted.webViewClientInstanceId("abrigo_gratuito_unesco") {
                        if sSubviewBottom!.count > 1 {
                            let iAsSubclass = sSubviewBottom![1] as! String
                            if iAsSubclass.count > 0 {
                                UoSerializeGroup.fasRightFromBracket(Int(iAsSubclass)!)
                            }
                        }
                    }else if xNsError == HeLoadPaletted.webViewClientInstanceId("livestreamd_macpascal_advocate") {
                        if sSubviewBottom!.count > 1 {
                            let iAsSubclass = sSubviewBottom![1] as! String
                            if iAsSubclass.count > 0 {
                                UoSerializeGroup.aliceMadeBobAdmin(Int(iAsSubclass)!)
                            }
                        }
                    }
                }
            }
        }else if message.name == HeLoadPaletted.webViewClientInstanceId("subtly_verilen") || message.name == HeLoadPaletted.webViewClientInstanceId("attribute_partitioned") {
            mdiArrowLeftBox()
        }
    }
        
    func mdiArrowLeftBox() {
        if uPathAccumulator == true {
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { [weak self] in
                self?.hAddFailure?.evaluateJavaScript(HeLoadPaletted.webViewClientInstanceId("fallbacking_mehod"),completionHandler: { a, err in
                })
            }
        }
    }
        
    func dllReleaseAll() {
        iAsSubclass = 0
        if hAddFailure != nil && hAddFailure.canGoBack {
            hAddFailure.goBack()
        }else {
            mdiFormatLetterStartsWith("")
        }
    }
    func symRoundedGoogleWifi(_ type : Int) {
        UoSerializeGroup.toolbarAllowedItemIdentifiers(type)
    }
        
    override func removeFromSuperview() {
        super.removeFromSuperview()
                
        hAddFailure?.evaluateJavaScript(HeLoadPaletted.webViewClientInstanceId("ijkplayer_aayk_rab"),completionHandler: { a, err in
        })
        capturedStatusBarProperties()
    }
    func setUniqueId() {
        self.removeFromSuperview()
        sProfileSharing?.removeFromSuperview()
        qClockListener = 0
        zBuildJson = 0
        eRecipientIdentities = ""
        iAsSubclass = 0
        uPathAccumulator = false
        zSdkConfigs = 0.0
        hAddFailure?.removeFromSuperview()
        hAddFailure = nil
    }
        
    static let iRemindersB : UoModifyDescriptionAction = UoModifyDescriptionAction.registerWhenCreateInstance()
}
